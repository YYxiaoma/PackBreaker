# PackBreaker v1.0.2 正式发布与受控恢复记录

正式发布时间：2026-09-23（UTC），GitHub Release 为非 Draft、非 Pre-release。本记录为发布后核验，不修改已发布 Tag、镜像或线上容器。

## 发行身份

- 注释 Tag：`v1.0.2`；解引用源码提交 `43be7bf77276f42d1dca45a4b78132d6a7044b30`。
- 正式镜像：`ghcr.io/yyxiaoma/packbreaker:1.0.2`，不可变 OCI Index 为 `sha256:eb6083864d3a60f0dc926ac58cc27ccb18405df5f766d66cbc533a0252539fd5`。
- 独立回读 GHCR 并重算 Index、子 manifest 和 config 哈希，确认 `linux/amd64` 与 `linux/arm64` 均真实存在，两个架构的 `org.opencontainers.image.version` 均为 `1.0.2`，`org.opencontainers.image.revision` 均为上述源码提交。
- `1.0.2`、`stable`、`latest` 现指向同一正式不可变摘要。历史 `1.0.1` 仍保留 `sha256:3fb0ab8cee39b245d8a2b6fedae1336afa41ff18cf380d5f448a1b1fef832cb2`。

## 正式发布与恢复

- 候选源码完整 CI：[run #35872851636](https://github.com/YYxiaoma/PackBreaker/actions/runs/35872851636)；候选双架构镜像交付：[run #35872851854](https://github.com/YYxiaoma/PackBreaker/actions/runs/35872851854)；默认分支 CI：[run #35873959998](https://github.com/YYxiaoma/PackBreaker/actions/runs/35873959998)；默认分支 Docker E2E：[run #35873959951](https://github.com/YYxiaoma/PackBreaker/actions/runs/35873959951)，均为 Success。
- 原始 [Release run #35875040822](https://github.com/YYxiaoma/PackBreaker/actions/runs/35875040822) 在已发布 ARM64 镜像的 QEMU 隔离运行检查失败；此前原生候选 ARM64、正式 AMD64 运行与 v1.0.1 → v1.0.2 升级回滚已成功。原始运行保留 Failure 状态，不能仅凭相同失败位置判定根因。
- [不可变摘要恢复 run #35877552693](https://github.com/YYxiaoma/PackBreaker/actions/runs/35877552693) 在三个独立 Runner 上重新验证**已推送的同一镜像**：AMD64 运行及相邻升级回滚、QEMU ARM64 运行、原生 ARM64 运行全部通过。资产 Job 成功创建 [v1.0.2 GitHub Release](https://github.com/YYxiaoma/PackBreaker/releases/tag/v1.0.2)，发布后下载验证三份资产，最后更新并核对 `stable/latest`。恢复未重新构建或覆盖正式镜像，未移动原始 Tag。

## 发布资产独立复核

从公开 GitHub Release 重新下载实测：

| 资产 | SHA-256 |
| --- | --- |
| `packbreaker-1.0.2.release.json` | `c973bcc277b125c5043c9d20c63b0ae70b307fcfcf3c7dd2ea880aa967f9e6bc` |
| `packbreaker-1.0.2.spdx.json` | `47e801318787b215e8be0476304cb3489ea23f17a226ea3a4a0b5d922e125db8` |
| `SHA256SUMS` | `f1c2a9d5660f0cd2bab3c80bf888d75f1a682f9fd4335b801021b0e0f7a197e5` |

`SHA256SUMS` 完整覆盖两份 JSON 且哈希一致；发布清单的 tag、version、commit、index 摘要、双架构声明及 SPDX SBOM 文件名/哈希均与实测匹配，SBOM 版本为 SPDX-2.3。

## 本版能力与未验收边界

- 添加站点可选择 KeepFrds、HDHome、UBits、HDFans、Rousi Pro、BTSCHOOL、PTTime、聆音Club 八站，允许按固定 Profile origin 加密保存凭据、执行只读连接测试；添加/编辑表单已删除站点地址输入框。
- 这八站仍为 `PENDING_REAL_VALIDATION`，默认停用，后端禁止启用正式辅种任务；并未通过完整真实辅种、下载器现场验收。Rousi Pro 独立详情和 HDFans 分页的既有未完成/失败证据不应改记成功。
- 本版配置开放的自动化测试包括完全合成的 MockTransport，不等于八站的真实任务支持。未读取真实站点凭据或对生产下载器、媒体执行写操作。

## 下一版本基线

发布后 `release-baseline.json` 应固定本次 `v1.0.2` 的 Tag、源码提交、不可变摘要和恢复 run `35877552693`；后续候选版本高于该基线时，必须重新运行正式相邻升级/回滚和 updater E2E。同版本日常 CI 不将自身升级伪装成跨版本成功。历史镜像、Release、失败与恢复记录均保留。
